import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.datascopedyad',
  appName: 'DataScopeDyad',
  webDir: 'dist'
};

export default config;
